# Setup Firebase — PakKom Eco Track v3.3.2

Upload seluruh isi v3.3.2 ke root GitHub Pages.

File JavaScript aplikasi yang digunakan hanya:
`main.js`

Hapus file lama berikut dari repository jika masih ada:
- auth.js
- app.js
- auth-core.js
- app-core.js

Firebase:
1. Authentication → Email/Password aktif.
2. Publish `firestore.rules`.
3. Admin Authentication: `komarudingalasta@gmail.com`.
4. `users/{UID_ADMIN}` memiliki `loginId: ADMIN`, `role: admin`, `active: true`.
