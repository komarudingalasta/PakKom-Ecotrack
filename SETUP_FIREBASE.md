# Setup PakKom Eco Track v3.3.3

1. Upload seluruh isi ZIP ke root repository.
2. Pastikan hanya `main.js` yang menjadi JavaScript aplikasi.
3. Firebase Authentication: Email/Password aktif.
4. Firestore Rules: publish `firestore.rules`.
5. Admin Authentication: `komarudingalasta@gmail.com`.
6. Firestore `users/{UID_ADMIN}`: `loginId=ADMIN`, `role=admin`, `active=true`.
